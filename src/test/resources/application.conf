database {
  connection {
    driver=org.h2.Driver
    url="jdbc:h2:C:/Temp/testDb"
    username=""
    password=""
    pool {
      initialSize=2
      maxTotal=5
      maxOpenPreparedStatements=50
      lifo=true
      validation="values 1"
    }
  }
}