database {
  connection {
    driver=org.apache.derby.jdbc.EmbeddedDriver
    url="jdbc:derby:testDatabase;databaseName=test;create=true"
    username=""
    password=""
    pool {
      initialSize=2
      maxTotal=5
      maxOpenPreparedStatements=50
      lifo=true
      validation="values 1"
    }
  }
}